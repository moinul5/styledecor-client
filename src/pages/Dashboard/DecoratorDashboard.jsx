const DecoratorDashboard = () => {
  return (
    <div>
      <h1 className="text-3xl font-bold font-serif mb-6">Decorator <span className="gradient-text">Dashboard</span></h1>
      <div className="text-base-content/40 py-10">
        <p>Decorator dashboard — Full implementation in Milestone 9</p>
      </div>
    </div>
  );
};

export default DecoratorDashboard;
